<div class="card">
  <h2><%= title %></h2>
  <p><%= message %></p>
  <p>This is a simple Server-Side Rendered (SSR) application that integrates the logme library for standardized logging.</p>
  <p>Check the server logs to see the structured log output with correlation IDs.</p>
  <h3>Features Demonstrated:</h3>
  <ul>
    <li>Express middleware for request/response logging</li>
    <li>Database operation logging (simulated)</li>
    <li>Error logging</li>
    <li>Correlation ID tracking across components</li>
  </ul>
  <p>Navigate to <a href="/users">Users</a> to see a list of users and database logging in action.</p>
</div> 